#include "parse.h"

#include <cstdio>
#include <cstdlib>
#include <cstring>

class Parse {
 public:
  std::string host;
  std::string port;
  std::string method;
  std::string log_info;
  std : string send;
  std::string input;

 public:
  Parse(std::string request) : input(request) {}
  void ParseInput();
};
